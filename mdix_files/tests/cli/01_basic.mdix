@CONFIG(
  version -> "1.0.0"
  author  -> "CLI Test — Basic"
)

@DATA(
  app_name = "TestApp"
  version  = "1.0.0"
  port     = 8080
  debug    = true
  timeout  = 5000

  server: host = "localhost", port = 8080, ssl = false

  tags:: "web", "api", "v1"
)
