{
  "project": {
    "name": "DixScript Rust Port",
    "subtitle": "MidManStudio Data Interchange Extension Script",
    "version": "1.0.0",
    "status": "Phase 2 - ErrorManager Complete, Starting Compiler",
    "repository": "https://github.com/Mid-D-Man/DixScript-Rust",
    "file_extension": ".mdix"
  },

  "⚡_CRITICAL_RULES_READ_FIRST": {
    "⭐_importance": "These rules are NON-NEGOTIABLE and prevent 90% of porting issues",

    "1_USE_NATIVE_RUST_RESULT": {
      "rule": "DO NOT create custom Result<T,E> wrapper - USE RUST'S BUILT-IN",
      "reason": "Rust's Result IS what the C# Result was modeled after",
      "wrong": "pub struct Result<T, E> { /* custom wrapper */ }",
      "right": "Use std::result::Result<T, E> directly",
      "dixscript_pattern": "pub type DixResult<T> = Result<T, DixError>;",
      "usage": {
        "ok": "Ok(value)",
        "err": "Err(DixError::ParseError(\"message\".to_string()))",
        "match": "match result {\n  Ok(val) => /* use val */,\n  Err(e) => /* handle error */\n}",
        "propagate": "let value = might_fail()?; // Auto-return Err if failed"
      }
    },

    "2_BORROWING_RULES": {
      "rule": "You can have EITHER many readers OR one writer, NEVER both",
      "the_golden_rule": "Aliasing XOR Mutability - many reads OR one write, not both",
      "immutable_borrows": {
        "syntax": "&value",
        "can_have": "UNLIMITED immutable borrows at once",
        "cannot_do": "Modify the value",
        "example": "fn read_data(data: &String) { println!(\"{}\", data); }"
      },
      "mutable_borrows": {
        "syntax": "&mut value",
        "can_have": "ONLY ONE mutable borrow at a time",
        "cannot_do": "Have other borrows (even immutable) while mutable exists",
        "example": "fn modify_data(data: &mut String) { data.push_str(\"!\"); }",
        "common_error": {
          "wrong": "let r1 = &mut data;\nlet r2 = &mut data; // ERROR!",
          "right": "{\n  let r1 = &mut data;\n  r1.push_str(\"!\");\n} // r1 scope ends\nlet r2 = &mut data; // OK now"
        }
      },
      "lifetime_basics": {
        "what": "References must NEVER outlive the data they point to",
        "when_needed": "When compiler can't infer (multiple inputs/outputs)",
        "syntax": "'a is a lifetime parameter",
        "example": "fn longest<'a>(s1: &'a str, s2: &'a str) -> &'a str",
        "meaning": "Output lives as long as BOTH inputs (shortest lifetime)"
      }
    },

    "3_OWNERSHIP_RULES": {
      "rule": "Each value has EXACTLY ONE owner. When owner drops, value is freed.",
      "three_ways_to_handle_data": {
        "1_move": {
          "what": "Transfer ownership (original becomes invalid)",
          "example": "let s1 = String::from(\"hello\");\nlet s2 = s1; // s1 MOVED, can't use anymore",
          "when": "Passing to function that consumes it",
          "cost": "Zero (just pointer reassignment)"
        },
        "2_borrow": {
          "what": "Temporary access WITHOUT ownership transfer",
          "example": "fn process(data: &String) { /* read only */ }",
          "when": "Function needs to READ but not own",
          "cost": "Zero (just a pointer)"
        },
        "3_clone": {
          "what": "Deep copy - creates NEW owned value",
          "example": "let s2 = s1.clone(); // Both valid, but expensive",
          "when": "ONLY when you actually need TWO independent copies",
          "cost": "Expensive (allocates and copies all data)",
          "warning": "⚠️ CLONING IS YOUR LAST RESORT, NOT YOUR FIRST"
        }
      }
    },

    "4_WHEN_TO_CLONE_VS_BORROW": {
      "⚠️_performance_impact": "Cloning can be 10-1000x slower than borrowing",
      "the_decision_tree": {
        "ask_yourself": [
          "1. Do I need to OWN this data? → Clone or move",
          "2. Do I just need to READ it? → Borrow (&T)",
          "3. Do I need to MODIFY it temporarily? → Mutable borrow (&mut T)",
          "4. Is the borrow checker complaining? → DON'T clone yet, understand why first"
        ]
      },
      "when_clone_is_ok": {
        "valid_reasons": [
          "Storing data in multiple independent collections",
          "Sending data to another thread (Arc<T> is better though)",
          "Creating a snapshot before modification",
          "Working around complex lifetime issues (after trying borrowing first)"
        ],
        "example_ok": "let snapshot = current_state.clone();\nmodify(&mut current_state);\nif bad { current_state = snapshot; }"
      },
      "when_clone_is_wrong": {
        "anti_patterns": [
          "❌ Cloning just to satisfy borrow checker without understanding",
          "❌ Cloning in hot loops (lexer, parser main loops)",
          "❌ Cloning when function only needs to read",
          "❌ Cloning large collections to iterate",
          "❌ Cloning in return values when reference would work"
        ],
        "example_bad": "fn process(data: String) { /* uses data.clone() everywhere */ }",
        "example_good": "fn process(data: &str) { /* just borrows */ }"
      },
      "specific_dixscript_guidance": {
        "lexer_tokens": "DON'T clone tokens in main loop - use Vec<Token> and borrow",
        "parser_state": "DON'T clone parse state - use &mut references",
        "error_collections": "OK to clone individual errors when adding to collection",
        "ast_nodes": "DON'T clone during traversal - use &Node or &mut Node",
        "symbol_tables": "Store owned values, return references",
        "string_literals": "Use &str for reading, String for ownership"
      }
    },

    "5_COPY_VS_CLONE": {
      "Copy": {
        "what": "Implicit bitwise copy (like C# value types)",
        "types": "i32, f64, bool, char, and other primitives",
        "behavior": "let y = x; // x is COPIED, both valid",
        "cost": "Zero (single CPU instruction)",
        "derive": "#[derive(Copy, Clone)] // Copy requires Clone"
      },
      "Clone": {
        "what": "Explicit deep copy (like C# .Clone())",
        "types": "String, Vec, complex structs",
        "behavior": "let y = x.clone(); // Explicit copy",
        "cost": "Expensive (allocates and copies data)",
        "derive": "#[derive(Clone)]"
      },
      "when_to_use": {
        "use_Copy_for": "Small structs with only Copy fields (Point, Token metadata)",
        "use_Clone_for": "Types with heap data (String, Vec) or large structs",
        "never_both_for": "Types with heap allocations (can't be Copy)"
      }
    }
  },

  "🎯_HYBRID_WRAPPER_STRATEGY": {
    "philosophy": "Use wrappers for ease of porting, native types for performance",
    "decision_matrix": {
      "use_dixcore_wrappers": {
        "where": [
          "ErrorManager collections (not in hot path)",
          "Configuration data (parsed once)",
          "Symbol tables (moderate frequency access)",
          "Quick prototyping during initial porting"
        ],
        "why": "Easier to port from C#, method names match exactly",
        "performance": "⚡ Wrappers have ZERO runtime cost (compiler inlines them)",
        "types": "List<T>, Dictionary<K,V>, HashSet<T>"
      },
      "use_native_rust": {
        "where": [
          "Lexer token buffer (hot path - accessed per character)",
          "Parser lookahead (hot path - accessed per token)",
          "AST node children (traversed constantly)",
          "Any loop that runs per-character or per-token"
        ],
        "why": "Direct access, no wrapper overhead, idiomatic Rust",
        "performance": "⚡⚡ Maximum performance, zero abstraction",
        "types": "Vec<T>, HashMap<K,V>, HashSet<T>, &[T] (slices)"
      }
    },
    "performance_reality": {
      "wrapper_overhead": "0-2% in release builds (compiler optimizes away)",
      "real_bottlenecks": [
        "❌ Cloning in loops (10-100x slowdown)",
        "❌ Unnecessary allocations (10x slowdown)",
        "❌ Bad algorithm choice (100-1000x slowdown)"
      ],
      "conclusion": "Wrapper vs native is negligible. Cloning and allocation matter WAY more."
    }
  },

  "📊_PERFORMANCE_GUIDELINES": {
    "hot_paths": {
      "definition": "Code executed thousands+ times per file (lexer loops, parser lookahead)",
      "rules": [
        "❌ NEVER clone in hot paths",
        "✅ Use slices (&[T]) instead of Vec<T>",
        "✅ Borrow, don't own",
        "✅ Reuse allocations (Vec::with_capacity)",
        "✅ Profile BEFORE optimizing (cargo flamegraph)"
      ]
    },
    "cold_paths": {
      "definition": "Code executed once or rarely (setup, error reporting)",
      "rules": [
        "✅ Clone is fine here",
        "✅ Readability > micro-optimization",
        "✅ Use String instead of &str for simplicity",
        "✅ Don't worry about wrapper overhead"
      ]
    },
    "specific_optimizations": {
      "avoid": {
        "collect_then_iterate": "items.iter().collect::<Vec<_>>().iter() // ❌ Wasteful",
        "repeated_cloning": "for item in &items { let owned = item.clone(); } // ❌ Bad",
        "string_concat_loop": "for s in &strings { result = result + s; } // ❌ O(n²)"
      },
      "prefer": {
        "lazy_iteration": "items.iter().filter(...).map(...) // ✅ No allocation",
        "borrowing": "for item in &items { process(item); } // ✅ Zero copy",
        "string_join": "strings.join(\"\") // ✅ Single allocation"
      }
    },
    "cow_pattern": {
      "what": "Clone-on-Write - borrows when possible, clones only when modified",
      "type": "Cow<'a, T> (std::borrow::Cow)",
      "use_when": "Data is USUALLY not modified, but SOMETIMES needs to be",
      "example": "use std::borrow::Cow;\nfn process(s: Cow<str>) -> Cow<str> {\n  if s.contains(\"bad\") {\n    Cow::Owned(s.replace(\"bad\", \"good\"))\n  } else {\n    s // No clone!\n  }\n}"
    }
  },

  "🔧_COMMON_ERRORS_AND_FIXES": {
    "value_moved_error": {
      "error": "value used after move",
      "cause": "Tried to use value after moving it",
      "example_wrong": "let s1 = String::from(\"hello\");\nlet s2 = s1;\nprintln!(\"{}\", s1); // ❌ s1 was moved",
      "fix_1_clone": "let s2 = s1.clone(); // Both valid",
      "fix_2_borrow": "let s2 = &s1; // Borrow instead",
      "fix_3_use_after": "println!(\"{}\", s1);\nlet s2 = s1; // Use before move"
    },
    "cannot_borrow_as_mutable": {
      "error": "cannot borrow as mutable because it is also borrowed as immutable",
      "cause": "Trying to mutate while immutable borrows exist",
      "example_wrong": "let r1 = &data;\nlet r2 = &mut data; // ❌ Can't have both",
      "fix": "// Ensure immutable borrow ends before mutable\n{\n  let r1 = &data;\n  println!(\"{}\", r1);\n} // r1 dropped\nlet r2 = &mut data; // ✅ OK now"
    },
    "cannot_move_out_of_borrowed": {
      "error": "cannot move out of `*list` which is behind a shared reference",
      "cause": "Trying to take ownership from a borrow",
      "example_wrong": "fn process(list: &List<String>) {\n  let item = list.First(); // ❌ Can't move\n}",
      "fix": "fn process(list: &List<String>) {\n  if let Some(item) = list.First() {\n    let owned = item.clone(); // Clone if need ownership\n    // Or just use &item\n  }\n}"
    }
  },

  "🏗️_PORTING_STRATEGY": {
    "approach": "Hybrid: Manual port with C# naming conventions where beneficial",
    "naming_decisions": {
      "rust_style_snake_case": {
        "what": "New Rust code uses standard Rust naming",
        "applies_to": [
          "All new modules (error_manager, lexer, parser)",
          "All functions (fn parse_token(), fn add_error())",
          "All local variables (let token_type, let error_count)",
          "All file names (error_manager.rs, lexical_error.rs)"
        ],
        "reason": "Standard Rust conventions, better tooling support"
      },
      "c_sharp_style_PascalCase": {
        "what": "DixCore and Utilities maintain C# naming during porting phase",
        "applies_to": [
          "DixCore types (List, Dictionary, HashSet)",
          "DixCore methods (Add, Remove, Where, Select)",
          "Utility types (Token, TokenType, MID_Logger)",
          "Utility methods (GetSharedInstance, CreateScope)"
        ],
        "reason": "Easier 1:1 mapping during initial port, minimize cognitive load"
      },
      "transition_plan": {
        "phase_1": "Use DixCore/Utilities with C# naming (CURRENT)",
        "phase_2": "Port compiler using Rust naming + DixCore",
        "phase_3": "Optionally add snake_case aliases to DixCore",
        "phase_4": "Maybe refactor to pure Rust style later (low priority)"
      }
    },

    "phases": [
      {
        "phase": 1,
        "name": "DixCore - C# Collection Wrappers",
        "status": "✅ COMPLETE",
        "files": [
          "src/DixCore/list.rs",
          "src/DixCore/dictionary.rs",
          "src/DixCore/hash_set.rs",
          "src/DixCore/immutable_array.rs",
          "src/DixCore/linq.rs"
        ]
      },
      {
        "phase": 2,
        "name": "Utilities",
        "status": "✅ COMPLETE",
        "files": [
          "src/Utilities/token.rs",
          "src/Utilities/keyword_definitions.rs",
          "src/Utilities/mid_logger.rs",
          "src/Utilities/mid_helper_functions.rs",
          "src/Utilities/utilities.rs"
        ]
      },
      {
        "phase": 3,
        "name": "ErrorManager",
        "status": "✅ COMPLETE",
        "files": [
          "src/ErrorManager/error_manager.rs",
          "src/ErrorManager/operational_settings.rs",
          "src/ErrorManager/ErrorTypes/*.rs (all 10 error types)"
        ],
        "note": "Uses Rust naming (snake_case), Result<T,E>, and DixCore wrappers"
      },
      {
        "phase": 4,
        "name": "Compiler - Lexer",
        "status": "⏳ NEXT",
        "files": ["src/Compiler/Core/lexer.rs"],
        "strategy": "Use Vec<Token> directly (hot path), borrow heavily",
        "c_sharp_ref": "DixScript/Compiler/Core/Lexer.cs"
      },
      {
        "phase": 5,
        "name": "Compiler - Parser",
        "status": "⏳ PENDING",
        "files": [
          "src/Compiler/Core/general_parser.rs",
          "src/Compiler/Core/SectionParsers/*.rs"
        ],
        "strategy": "AST with owned nodes, borrow during traversal"
      },
      {
        "phase": 6,
        "name": "Compiler - Semantic Analysis",
        "status": "⏳ PENDING",
        "files": [
          "src/Compiler/Core/general_semantics_analyzer.rs",
          "src/Compiler/Core/SectionAnalyzers/*.rs"
        ]
      }
    ],

    "step_by_step_porting_workflow": {
      "1_understand_c_sharp": "Read C# code, understand algorithm",
      "2_identify_types": "What types does it use? (List, Dictionary, Token)",
      "3_check_hot_path": "Is this called in a loop? (Yes = native types, No = wrappers OK)",
      "4_create_skeleton": "Create struct with pub fields",
      "5_port_methods": "One method at a time, test each",
      "6_handle_ownership": {
        "reading": "Use &T (borrow)",
        "modifying": "Use &mut T (mutable borrow)",
        "consuming": "Take ownership (move)",
        "last_resort": "Clone if truly needed"
      },
      "7_test_incrementally": "cargo test after each method",
      "8_fix_errors": "Read error messages carefully, they're helpful",
      "9_profile_if_slow": "cargo flamegraph to find real bottlenecks",
      "10_iterate": "Refine and optimize based on benchmarks"
    }
  },

  "🚀_FFI_STRATEGY": {
    "overview": "One Rust core → Multiple language bindings",
    "platforms": {
      "desktop": ["Windows", "macOS", "Linux"],
      "mobile": ["iOS", "Android"],
      "web": ["WebAssembly"],
      "game_engines": ["Unity", "Godot", "Unreal"],
      "blockchain": ["Solana", "Ethereum"]
    },
    "tools": {
      "c_api": {
        "what": "extern \"C\" functions for stable ABI",
        "file": "src/ffi.rs",
        "example": "extern \"C\" fn dix_parse(source: *const c_char) -> *mut DixHandle"
      },
      "csharp": {
        "tool": "csbindgen",
        "install": "cargo install csbindgen",
        "generate": "csbindgen --crate dixscript --output bindings/csharp/",
        "use_in_unity": "[DllImport(\"dixscript\")]"
      },
      "javascript": {
        "tool": "wasm-bindgen + wasm-pack",
        "install": "cargo install wasm-pack",
        "build": "wasm-pack build --target web",
        "output": "pkg/dixscript.js + dixscript_bg.wasm"
      },
      "python": {
        "tool": "PyO3",
        "add": "[dependencies]\npyo3 = \"0.20\"",
        "build": "maturin develop"
      },
      "multi_language": {
        "tool": "UniFFI (recommended for 3+ languages)",
        "what": "Single .udl definition → Kotlin, Swift, Python, Ruby auto-generated",
        "add": "[dependencies]\nuniffi = \"0.25\""
      }
    },
    "size_estimates": {
      "base_tools": "Built-in FFI (0 MB - part of std)",
      "csbindgen": "~2 MB",
      "wasm_pack": "~15 MB",
      "pyo3": "~8 MB",
      "uniffi": "~5 MB",
      "total_optional": "~30 MB (only install what you need)"
    }
  },

  "⚡_PERFORMANCE_BENCHMARKING": {
    "setup": {
      "add_to_cargo_toml": {
        "dev_dependencies": "criterion = \"0.5\"",
        "bench_section": "[[bench]]\nname = \"wrapper_benchmark\"\nharness = false"
      },
      "create_file": "benches/wrapper_benchmark.rs",
      "run": "cargo bench"
    },
    "what_to_benchmark": {
      "wrappers_vs_native": "List<T> vs Vec<T> add/iterate",
      "clone_vs_borrow": "Cloning in loop vs borrowing",
      "iteration_patterns": ".iter() vs .into_iter() vs .Iter()",
      "hot_path_operations": "Token buffer operations, parser lookahead"
    },
    "interpretation": {
      "< 5%_difference": "Negligible, choose for readability",
      "5_20%_difference": "Noticeable, consider native types",
      "> 20%_difference": "Significant, use native types or optimize algorithm"
    }
  },

  "📚_RUST_SYNTAX_QUICK_REFERENCE": {
    "return_types": {
      "-> Self": "Returns instance of current type (like C# 'return this;' type)",
      "-> ()": "Void/Unit (empty tuple, often omitted)",
      "-> Result<T, E>": "Operation that can succeed (Ok) or fail (Err)",
      "-> Option<T>": "Value that might be None",
      "no_arrow": "Implicitly returns () (void)"
    },
    "closures": {
      "|x| x * 2": "Lambda: x => x * 2",
      "|x, y| x + y": "Lambda: (x, y) => x + y",
      "|_| 42": "Lambda ignoring param: _ => 42",
      "|x| { let y = x * 2; y + 1 }": "Multi-line lambda with block"
    },
    "result_option": {
      "Ok(value)": "Success case of Result",
      "Err(error)": "Error case of Result",
      "Some(value)": "Present case of Option",
      "None": "Absent case of Option",
      "?": "Early return on Err (propagate error up)"
    },
    "derive_macros": {
      "#[derive(Debug)]": "Auto-implement Debug trait (like ToString)",
      "#[derive(Clone)]": "Auto-implement Clone trait (deep copy)",
      "#[derive(Copy)]": "Auto-implement Copy trait (bitwise copy, requires Clone)",
      "#[derive(PartialEq)]": "Auto-implement == and != operators",
      "#[derive(Default)]": "Auto-implement default constructor",
      "multiple": "#[derive(Debug, Clone, PartialEq)]"
    },
    "references": {
      "&value": "Immutable borrow/reference (read-only)",
      "&mut value": "Mutable borrow/reference (can modify)",
      "*reference": "Dereference (get value from reference)",
      "&&value": "Reference to reference (rare, usually auto-deref)"
    }
  },

  "🔍_DEBUGGING_AND_TOOLING": {
    "cargo_commands": {
      "cargo check": "Fast compile check (no binary)",
      "cargo build": "Debug build (fast, unoptimized)",
      "cargo build --release": "Release build (slow, optimized)",
      "cargo test": "Run all tests",
      "cargo test test_name": "Run specific test",
      "cargo bench": "Run benchmarks",
      "cargo clippy": "Lint (find common mistakes)",
      "cargo fmt": "Auto-format code"
    },
    "debugging_macros": {
      "println!(\"x = {:?}\", x)": "Print debug representation",
      "dbg!(x)": "Print and return value (inline debugging)",
      "eprintln!(\"error: {}\", e)": "Print to stderr",
      "panic!(\"msg\")": "Crash with message"
    },
    "profiling": {
      "install": "cargo install flamegraph",
      "run": "cargo flamegraph --bench wrapper_benchmark",
      "output": "flamegraph.svg (open in browser)"
    }
  },

  "💡_TIPS_FOR_C_SHARP_DEVELOPERS": {
    "mindset_shifts": {
      "no_null": "Use Option<T> instead of null",
      "no_exceptions": "Use Result<T, E> instead of try/catch",
      "no_gc": "Ownership system instead of garbage collector",
      "immutable_default": "Use 'mut' to make variables mutable",
      "explicit_copies": "Clone is explicit, not automatic"
    },
    "common_patterns": {
      "c_sharp_null_check": "if (value != null)",
      "rust_equivalent": "if let Some(v) = value",
      "c_sharp_try_catch": "try { x() } catch (Exception e) { }",
      "rust_equivalent": "match x() { Ok(v) => {}, Err(e) => {} }",
      "c_sharp_foreach": "foreach (var item in items)",
      "rust_equivalent": "for item in &items"
    },
    "when_stuck": [
      "1. Read error message CAREFULLY (Rust errors are exceptionally helpful)",
      "2. Check if you need ownership, mutable borrow, or just read access",
      "3. Search: 'rust [error code]' (e.g., 'rust E0502')",
      "4. Run 'cargo clippy' - it catches common mistakes",
      "5. Ask: 'Why is Rust preventing this?' (usually a real safety issue)"
    ]
  },

  "🎯_NEXT_STEPS": {
    "completed": [
      "✅ DixCore collection wrappers",
      "✅ Utilities (Token, Logger, Keywords)",
      "✅ ErrorManager (all 10 error types)",
      "✅ Consolidated porting strategy",
      "✅ Ownership/borrowing/lifetime research"
    ],
    "immediate_next": [
      "1. Benchmark DixCore wrappers vs native (verify performance)",
      "2. Fix ErrorManager move errors (return references, not clones)",
      "3. Port Lexer using native Vec<Token> (hot path optimization)",
      "4. Port Parser with AST using owned nodes",
      "5. Continuous testing and profiling"
    ],
    "files_to_reference": {
      "this_strategy": "DixScript_Rust_Porting_Strategy.json",
      "c_sharp_lexer": "DixScript/Compiler/Core/Lexer.cs",
      "c_sharp_parser": "DixScript/Compiler/Core/GeneralParser.cs",
      "rust_error_manager": "src/ErrorManager/error_manager.rs",
      "rust_dixcore": "src/DixCore/mod.rs"
    }
  },

  "📖_RESOURCES": {
    "official_docs": [
      "The Rust Book: https://doc.rust-lang.org/book/",
      "Rust by Example: https://doc.rust-lang.org/rust-by-example/",
      "Rust docs.rs: https://docs.rs/",
      "Ownership explained: https://doc.rust-lang.org/book/ch04-00-understanding-ownership.html"
    ],
    "performance": [
      "Rust Performance Book: https://nnethercote.github.io/perf-book/",
      "Criterion benchmarking: https://bheisler.github.io/criterion.rs/book/",
      "Flamegraph: https://github.com/flamegraph-rs/flamegraph"
    ],
    "best_practices": [
      "Rust Design Patterns: https://rust-unofficial.github.io/patterns/",
      "API Guidelines: https://rust-lang.github.io/api-guidelines/",
      "Clippy lints: https://rust-lang.github.io/rust-clippy/"
    ]
  }
}